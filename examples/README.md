# Demo artifacts

`demo_logs/` is generated by `losslint demo examples/demo_logs` and is not
committed; run that command to regenerate everything (six logs, including a
TensorBoard event file when the `[tb]` extra is installed), then:

```
losslint check examples/demo_logs
```

Expected: 7 findings across 6 runs, exit code 1.
